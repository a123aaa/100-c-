#include<stdio.h>
#include<math.h>
int main()
{
    int i;
    for(i=100;i<1000;i++)
    {
        int a = i / 100, b = i % 10, c = i / 10 % 10;
        if (i == pow(a, 3) + pow(b, 3) + pow(c, 3))
            printf("%3d ",i);
    }
    return 0;
}
