int main()
{
	int  b = 1, x;
	float sum = 0,a=2.0;
	printf("输入要求前几项和=>");
	scanf("%d", &x);
	for(int i = 0; i < x; i++)
	{
		sum += a / b;                
		int c = a; a += b; b = c;        
	}
	printf("前%d项的和是%lf\n",x,sum);
	return 0;
}
